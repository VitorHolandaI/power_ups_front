services:
  voltage-monitor:
    build: .
    ports:
      - "8967:5000"
    volumes:
      - .:/app
    restart: "always"
    environment:
      - FLASK_ENV=development
      - UPS_DASHBOARD_HOST=0.0.0.0
